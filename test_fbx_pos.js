const fs = require('fs'); const content = fs.readFileSync('3D - Morth Targets/VO390/VO390_3.fbx', 'utf8'); ['Bone_Cuve1', 'Bone_Cuve2', 'Bone_Cuve3'].forEach(b => { const idx = content.indexOf(b); if (idx > -1) { const snippet = content.substring(idx, idx + 500); const match = snippet.match(/Lcl Translation.*?, (-?\d+\.?\d*), (-?\d+\.?\d*), (-?\d+\.?\d*)/); if (match) console.log(b, match[1], match[2], match[3]); } });
